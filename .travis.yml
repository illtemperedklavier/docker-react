sudo: required
services: 
  - docker 

before_install:
   - docker build -t alecr/docker-react -f Dockerfile.dev . 

language: generic 

script:
   - docker run -e CI=true alecr/docker-react npm run test